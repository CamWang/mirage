{
  "name": "mirage",
  "version": "1.0.0",
  "description": "An online judge platform",
  "main": "./backend/src/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "env": "cd frontend && npm install && cd ../backend && npm install && cd ../core && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:core": "cd core && npm run build",
    "build": "cd backend && npm build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs",
    "docs:deploy": "bash docs/deploy.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CamWang/mirage.git"
  },
  "author": "cam",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CamWang/mirage/issues"
  },
  "homepage": "https://github.com/CamWang/mirage#readme",
  "devDependencies": {
    "vitepress": "^0.15.6",
    "vuepress-bar": "^0.4.2"
  }
}
